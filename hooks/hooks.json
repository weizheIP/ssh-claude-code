{
  "description": "ssh-claude-code: SSH remote development keyword triggers",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "ssh|remote|远端|连接远程|ssh-connect|ssh-folder|ssh-disconnect",
        "hooks": [
          {
            "type": "command",
            "command": "",
            "timeout": 1
          }
        ]
      }
    ]
  }
}
